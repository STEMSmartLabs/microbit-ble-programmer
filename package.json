{
  "name": "stem-smart-labs-microbit-ble-programmer",
  "version": "2.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check compatibility.js && node --check core.js",
    "serve": "python3 -m http.server 8000"
  }
}
